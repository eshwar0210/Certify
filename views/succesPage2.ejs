<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    /* Style to center the content on the page */
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-color: #f4f4f4;
    }

    .container {
      text-align: center;
    }

    .alert {
      max-width: 500px;
      margin: 0 auto;
      padding: 30px;
      border-radius: 8px;
    }

    /* Style for the button */
    .btn-primary {
      background-color: black;
      border: none;
      color: white;
      padding: 10px 20px;
      font-size: 16px;
      border-radius: 5px;
    }

    .btn-primary:hover {
      background-color: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="alert alert-success" role="alert">
      <h4 class="alert-heading"><%= message %></h4>
     
      <hr />
      <p><strong>Certificate Address:</strong> <%= address %></p>
      <a href="/institute/<%= instituteadd %>" class="btn btn-primary">Go to Dashboard</a>
    </div>
  </div>
</body>
</html>
